<main>
    <section class="tarefas">
        {{#each tarefas }}
        <div class="tarefa">
            <form action="">
                {{#if this.completa}}
                <div class="completa">
                    <input type="checkbox" id="Tarefa-{{this.id}}" onchange="descompletartarefa({{this.id}})">
                    <label for="tarefa-{{this.id}}">{{this.descricao}}</label>
                </div>
                {{else}}
                <div>
                    <input type="checkbox" id="Tarefa-{{this.id}}" onchange="completartarefa({{this.id}})">
                    <label for="tarefa-{{this.id}}">{{this.descricao}}</label>
                </div>
                {{/if}}

                <button>
                    <img src="/imagens/x-icon.png" alt="icone de X para excluir uma tarefa">
                </button>
            </form>
        </div>

        {{/each}}
        <div class="informacoes">
            <p>{{quantidadeTarefasAtivas}} Tarefas Ativas</p>

            <div class="botoes">
                <a href="" class="active">Todas</a>
                <a href="">Ativas</a>
                <a href="">Completas</a>
                <a href="">Limpar Tarefas</a>
            </div>
            

        </div>
    </section>

    <section class="botoes">
        <a href="" class="active">Todas</a>
        <a href="">Ativas</a>
        <a href="">Completas</a>
    </section>
</main>